@model LibraryManagement.Models.TacGia

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Xóa</h1>

<div class="card mb-3">
    <div class="card-body">
        <h5 class="card-title">Xóa tác giả: @Model.TenTacGia</h5>
        <p class="text-body-secondary">Bạn có chắc chắn muốn xóa tác giả này? Hành động này có thể ảnh hưởng tới các sách liên quan.</p>

        <dl class="row">
            <dt class="col-sm-3">Tên</dt>
            <dd class="col-sm-9">@Model.TenTacGia</dd>

            <dt class="col-sm-3">Ngày sinh</dt>
            <dd class="col-sm-9">@Model.NgaySinh?.ToString("dd/MM/yyyy")</dd>

            <dt class="col-sm-3">Quốc tịch</dt>
            <dd class="col-sm-9">@Model.QuocTich</dd>

            <dt class="col-sm-3">Mô tả</dt>
            <dd class="col-sm-9">@Model.MoTa</dd>
        </dl>

        <form asp-action="Delete" method="post" class="d-inline">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">Xóa</button>
        </form>
        <a asp-action="Index" class="btn btn-secondary ms-2">Hủy</a>
    </div>
</div>
